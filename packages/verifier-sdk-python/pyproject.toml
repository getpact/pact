[project]
name = "getpact-verifier"
version = "0.0.0"
description = "Placeholder package for the future Pact Python verifier SDK"
license = "MIT"
requires-python = ">=3.11"
readme = "README.md"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/getpact_verifier"]
